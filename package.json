{
  "name": "buzz-workspace",
  "private": true,
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "setup:web": "bash scripts/setup-web.sh",
    "dev:web": "pnpm --dir web dev:shared",
    "check:web": "pnpm --dir web check && node --test scripts/development.test.mjs deploy/cloudflare/development-worker.test.mjs && node deploy/supabase/migration-manifest.mjs --check && pnpm --dir web build:preview",
    "test:web": "pnpm --dir web exec playwright test --config playwright.managed.config.ts",
    "doctor": "node scripts/doctor-web.mjs",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19"
  }
}
